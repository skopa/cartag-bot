import { SubscriptionConfig } from './subscription-manager.interfaces';

/**
 * Base subscriptions configuration
 */
export const baseSubscription: SubscriptionConfig = {
    free_recognitions_per_month: 100,
    default_ads_text: 'Don`t wait, add your plate @cartag_bot',
};
